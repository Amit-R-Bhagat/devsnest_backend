/**
 *
 * @param {string} name username
 * @param {string} email useremail
 * @param {number} phone contact no.
 * @returns
 */
function test(name, email, phone) {
  return {
    name,
    email,
    phone,
  };
}
